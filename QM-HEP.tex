\documentclass[onecolumn, amsmath, amssymb, aps, prd]{revtex4-2}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{dcolumn}
\usepackage{bm}
\usepackage{hyperref}
\usepackage[utf8]{inputenc}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=blue,
    citecolor=blue,
}

\title{Quantum Tomography for High Energy Physics}

% \author{F. Aguilar-Carvajal}
% \email{faguilar816@gmail.com}
% \affiliation{Departamento de Física, Universidad Técnica Federico Santa María, Campus San Joaquín}
\date{May 2026}

\begin{document}

\maketitle

\section{Quantum Tomography at work}
	\subsection{One Spin-1/2 system}
		Let's describe the one spin-1/2 density matrix:
		\begin{equation}
			\rho = \frac{1}{2} \left( \mathcal{I} + \vec{B} \cdot \vec{\sigma} \right)
		\end{equation}

		If we want to analyze a decay $\tau / \overline{\tau} \rightarrow l^+ / l^-$, then the option of meassure just two directions (up/down) isn't available, instead we need to meassure in a space, in a solid angle space. So, we have the intention of meassure in a specific direction in which the product of the decay goes on, say $\hat{n}$, then the proyector operator will take the form:
		\begin{equation}
			\Pi_{\hat{n}} = \frac{1}{2} \left( \mathcal{I} + \hat{n} \cdot \vec{\sigma} \right)
		\end{equation}

		The probability that the decay product be emitted in the $\hat{n}$ direction is:
		\begin{equation}
			\begin{split}
				\text{p}(\hat{n}) & = \text{Tr} \left[ \frac{1}{2} \left( \mathcal{I} + \vec{B} \cdot \vec{\sigma} \right) \frac{1}{2} \left( \mathcal{I} + \hat{n} \cdot \vec{\sigma} \right) \right] \\
				& = \text{Tr} \left[ \frac{1}{4} \left( \mathcal{I} + \vec{B} \cdot \vec{\sigma} + \hat{n} \cdot \vec{\sigma} + \underbrace{(\vec{B} \cdot \vec{\sigma})(\hat{n} \cdot \vec{\sigma})}_{(\vec{B} \cdot \hat{n})I + i(\vec{B} \times \hat{n})\cdot \vec{\sigma}} \right) \right] \\
				& = \frac{1}{4} \text{Tr} \left[ \mathcal{I} + \vec{B} \cdot \vec{\sigma} + \hat{n} \cdot \vec{\sigma} + (\vec{B} \cdot \hat{n})I + i(\vec{B} \times \hat{n})\cdot \vec{\sigma} \right] \\
				& = \frac{1}{4} \text{Tr} \left[ \mathcal{I} + \left( \vec{B} + \hat{n} + i(\vec{B} \times \hat{n}) \right) \cdot \vec{\sigma} + (\vec{B} \cdot \hat{n}) \mathcal{I} \right] \\
				& = \frac{1}{4} \left[ \underbrace{\text{Tr} \left[ \mathcal{I} \right]}_{4} + \underbrace{\text{Tr} \left[ \left( \vec{B} + \hat{n} + i(\vec{B} \times \hat{n}) \right) \cdot \vec{\sigma} \right]}_{\vec{\sigma} \text{'s are traceless}} + \text{Tr} \left[ (\vec{B} \cdot \hat{n})I \right]  \right] \\
				& = \frac{1}{4} \left[ 2 + 2 \vec{B} \cdot \hat{n} \right]
			\end{split}
		\end{equation}

		So, the probability is given by:
		\begin{equation}
			\text{p}(\hat{n}) = \frac{1}{2} \left[ 1 + \vec{B}\cdot \hat{n} \right]
		\end{equation}

		However, the probability isn't normalized in the solid angle space. Let's say that $\text{P} (\hat{n}) \propto 1 +\vec{B}\cdot \hat{n}$, then:
		\begin{align}
			\int d\Omega \ \text{p} (\hat{n}) = 1 \\
			\int d\Omega \left[ c (1+\vec{B}\cdot\hat{n}) \right] = 1 \\
			c \underbrace{\int d\Omega}_{4\pi} + c\vec{B} \cdot \underbrace{\int d\Omega \ \hat{n}}_{0} = 1 \\
			c = \frac{1}{4\pi}
		\end{align}

		Finally, the probability density is given by:
		\begin{equation}
			\text{p} (\hat{n}) = \frac{1}{4\pi} \left[ 1 + \vec{B} \cdot \hat{n} \right]
		\end{equation}

		Finally, to perform Quantum Tomography we need to include a \textit{Spin-analyzing power} parameter, $\kappa$, in order to cunatify how efficiently the $\hat{n}$ direction tracks the original polarization of the mother particle $\vec{B}$.
		\begin{equation}
			\text{p}(\hat{n}) = \frac{1}{4\pi} \left[ 1 + \kappa \vec{B} \cdot \hat{n} \right]
		\end{equation}

	\subsection{Important step to further probe}
		An important step that isn't detailled in the literature, and also I still don't completelly understand it, is that to obtain a full probability density function that the decay product be projected ($\hat{n}$) in a particular direction (+ or -), is multiply the probabilities of each path; that is:
		\begin{align}
			\text{p}(\hat{n}^+, \hat{n}^-) = \frac{1}{4\pi} \left[ 1+\kappa_+ B_i^+ n_i^+ \right] \frac{1}{4\pi} \left[ 1 + \kappa_- B_j^- n_j^- \right] \\
			\text{p}(\hat{n}^+, \hat{n}^-) = \frac{1}{(4\pi)^2} \left[ 1 + \kappa_+ B_i^+ n_i^+ + \kappa_- B_j^- n_j^- + \kappa_+ \kappa_- \sum_{i,j} C_{ij} n_i^+ n_j^- \right]
		\end{align}

	\subsection{Expected values}
		Since we already have an expression for the probability density function, the next step is try to find an expected value for a particular direction for the decay product.

		\subsubsection{$\langle \hat{n}^+ \rangle$}
			\begin{equation}
				\begin{split}
					\langle n_i^+ \rangle & = \int d\Omega^+ d\Omega^- n_l^+ \text{p}(n^+, n^-) \\
					& = \int d\Omega^+ d\Omega^- n_l^+ \frac{1}{(4\pi)^2} \left[ 1 + \kappa_+ B_i^+ n_i^+ + \kappa_- B_j^- n_j^- + \kappa_+ \kappa_- \sum_{i,j} C_{ij} n_i^+ n_j^- \right] \\
					& = \frac{1}{(4\pi)^2} \int d\Omega^+ d\Omega^- \left[ n_l^+ + \kappa_+ B_i^+ n_l^+ n_i^+ + \kappa_- B_j^- n_l^+ n_j^- + \kappa_+ \kappa_- \sum_{i,j} C_{ij} n_k^+ n_i^+ n_j^- \right] \\
					& = \frac{1}{(4\pi)^2} \int d\Omega^+ d\Omega^- \left[ \underbrace{n_l^+}_{(1)} + \underbrace{\kappa_+ B_i^+ n_l^+ n_i^+}_{(2)} + \underbrace{\kappa_- B_j^- n_l^+ n_j^-}_{(3)} + \underbrace{\kappa_+ \kappa_- \sum_{i,j} C_{ij} n_l^+ n_i^+ n_j^-}_{(4)} \right]
				\end{split}
			\end{equation}

			\begin{equation}
				\begin{split}
					(1): \ \ \ & \int d\Omega^+ d\Omega^- n_l^+ = \int \underbrace{d\Omega^-}_{4\pi} \int \underbrace{d\Omega^+ n_l^+}_{0} = 0 \\
					(2): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_+ B_i^+ n_l^+ n_i^+ = \kappa_+ B_i^+  \int \underbrace{d\Omega^-}_{4\pi} \int \underbrace{d\Omega^+ n_l^+ n_i^+}_{\frac{4\pi}{3}\delta_{li}} = \frac{(4\pi)^2}{3} \kappa_+ B_l^+ \\
					(3): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_- B_j^- n_l^+ n_j^- = \kappa_- B_j^- \int \underbrace{d\Omega^+ n_l^+}_{0} \int \underbrace{d\Omega^- n_j^-}_{0} = 0 \\
					(4): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_+ \kappa_- \sum_{i,j} C_{ij} n_l^+ n_i^+ n_j^- = \kappa_+ \kappa_- \sum_{i,j} C_{ij} \int d\Omega^+ n_l^+ n_i^+ \int \underbrace{d\Omega^- n_j^-}_{0} = 0
				\end{split}
			\end{equation}

			So, the expected value for the direction $n_i^+$ for the decay product is:
			\begin{equation}
				\langle n_i^+ \rangle = \frac{\kappa_+ B_i^+}{3}
			\end{equation}

			or, generally:
			\begin{equation} \label{eq1}
				\langle n_i^{\pm} \rangle = \frac{\kappa_{\pm} B_i^{\pm}}{3} \rightarrow B_i^{\pm} = \frac{3}{\kappa_{\pm}} \langle n_i^{\pm} \rangle
			\end{equation}

			This $B_i^{\pm}$ coefficient it's a very usefull for the framework developed in this work. This is due to the contribution to find the density matrix of a two spin-1/2 system. Now, we still need to find an expression for the expected value for the projected direction of the decay product. Since we already have an expression to find it, there is another way to perform the calculation of this projected direction?
			\begin{equation}
				\langle n_i^+ \rangle = \int d\Omega^+ d\Omega^- n_i^+ \underbrace{\text{p}(n^+, n^-)}_{\text{another expression?}}
			\end{equation}

		\subsubsection{Find another $\text{p}(n^+, n^-)$}
			In the particle physics context, the probability of produce a pair $\tau$/$\overline{\tau}$ and then decay to a product in the +/- direction came given by the cross section fo the proces. In this sense,
			\begin{equation}
				\int d\Omega^+ d\Omega^- \frac{d^2\sigma}{d\Omega^+ d\Omega^-} = \int d\sigma = \sigma_{total}
			\end{equation}

			For a probability density function, this result should be normalized:
			\begin{equation}
				\int d\Omega^+ d\Omega^- \frac{1}{\sigma_{total}} \frac{d^2\sigma}{d\Omega^+ d\Omega^-} = 1 \rightarrow \text{p}(n^+, n^-) \propto \frac{1}{\sigma} \frac{d^2\sigma}{d\Omega^+ d\Omega^-}
			\end{equation}

			For instance, we can take this expression and perform some computation to get the expected value for a projected particular direction:
			\begin{equation}
				\begin{split}
					\langle n_i^+ \rangle & = \int d\Omega^+ d\Omega^- n_i^+ \frac{1}{\sigma} \frac{d^2\sigma}{d\Omega^+ d\Omega^-} \\
					& = \frac{1}{\sigma} \int d\Omega^+ n_i^+ \frac{d\sigma}{d\Omega^+}
				\end{split}
			\end{equation}

			or, generally,
			\begin{equation}
				\langle n_i^{\pm} \rangle = \frac{1}{\sigma} \int d\Omega^{\pm} n_i^{\pm} \frac{d\sigma}{d\Omega^{\pm}}
			\end{equation}


			Let's replace this expression in~[\ref{eq1}]:
			\begin{equation}
				B_i^{\pm} = \frac{3}{\kappa_{\pm}} \frac{1}{\sigma} \int d\Omega^{\pm} n_i^{\pm} \frac{d\sigma}{d\Omega^{\pm}}
			\end{equation}

		\subsection{$\langle n_i^+ n_j^- \rangle$}
			Now, we will try to find another observable in this framework, something that can be meassure or can serve in order to obtain a complete description of the density matrix. Let's see the expected value of a product of projections:
			\begin{equation}
				\begin{split}
					\langle n_i^+ n_j^- \rangle & = \int d\Omega^+ d\Omega^- n_i^+ n_j^- \text{p}(n^+, n^-) \\
					& = \int d\Omega^+ d\Omega^- n_i^+ n_j^- \frac{1}{4\pi} \left[ 1 + \kappa_+ B_l^+ n_l^+ + \kappa_- B_m^- n_m^- + \kappa_+ \kappa_- \sum_{l,m} C_{lm} n_l^+ n_m^- \right] \\
					& = \frac{1}{4\pi} \int d\Omega^+ d\Omega^- \left[ \underbrace{n_i^+ n_j^-}_{(1)} + \underbrace{\kappa_+ B_l^+ n_i^+ n_j^- n_l^+}_{(2)} + \underbrace{\kappa_- B_m^- n_i^+ n_j^- n_m^-}_{(3)} + \underbrace{\kappa_+ \kappa_- \sum_{l,m} C_{lm} n_i^+ n_j^- n_l^+ n_m^-}_{(4)} \right] \\
				\end{split}
			\end{equation}

			\begin{equation}
				\begin{split}
					(1): \ \ \ & \int d\Omega^+ d\Omega^- n_i^+ n_j^- = \int \underbrace{d\Omega^- n_j^-}_{0} \int \underbrace{d\Omega^+ n_i^+}_{0} = 0 \\
					(2): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_+ B_l^+ n_i^+ n_j^- n_l^+ = \kappa_+ B_l^+ \int d\Omega^+ n_i^+ n_l^+ \int \underbrace{d\Omega^-  n_j^-}_{0} = 0 \\
					(3): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_- B_m^- n_i^+ n_j^- n_m^- = \kappa_- B_m^- \int \underbrace{d\Omega^+ n_i^+}_{0} \int d\Omega^- n_j^- n_m^- = 0 \\
					(4): \ \ \ & \int d\Omega^+ d\Omega^- \kappa_+ \kappa_- \sum_{l,m} C_{lm} n_i^+ n_j^- n_l^+ n_m^- = \kappa_+ \kappa_- \sum_{l,m} C_{lm} \int \underbrace{d\Omega^+ n_i^+ n_l^+}{\frac{4\pi}{3} \delta_{il}} \int \underbrace{d\Omega^- n_j^- n_m^-}_{\frac{4\pi}{3} \delta_{jm}} \\
					& = \left( \frac{4\pi}{3} \right)^2 \kappa_+ \kappa_- \sum_{l,m} C_{lm} \delta_{jm} \delta_{il} = \left( \frac{4\pi}{3} \right)^2 \kappa_+ \kappa_- C_{ij}
				\end{split}
			\end{equation}

			So,
			\begin{align}
				\langle n_i^+ n_j^- \rangle = \frac{\kappa_+ \kappa_-}{9} C_{ij} \\
				C_{ij} = \frac{9}{\kappa_+ \kappa_-} \langle n_i^+ n_j^- \rangle
			\end{align}










\end{document}
